#!/bin/sh
set -eu

while read -r local_ref local_sha remote_ref remote_sha; do
  case "$local_ref" in
    "") continue ;;
  esac
  node scripts/require-version-bump.mjs "$remote_sha" "$local_sha"
done
